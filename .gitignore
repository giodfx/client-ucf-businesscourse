node_modules/
*.docx
*.pdf
ingested/
sourceinfo/
poc/
designideas/
preview/
Feedback/
export/
exports/
validation-reports/
__pycache__/
.process-tracker.json
.gates/
_fix-*.mjs

# Heavy binaries — keep local, exclude from git
.archive-*/
.latam-removed-backup-*/
.deprecated-video-data-*/
**/.es-backup-*/
media/video/
media/audio/scenes/
media/audio/voice-tests/
media/audio/bookend/
# Bookend video binaries stay out of git (~145MB total) — pushed to R2 via
# cf:r2:upload. Small text assets the player needs (captions.js + .vtt
# transcripts) ARE tracked so the cloud regen can produce HTML that finds them.
output/lessons/videos/**/*.mp4
output/lessons/videos/**/*.webm
output/lessons/videos/**/*.mov

# Editor / runtime artifacts
.claude/
tmp/

# Process logs
*.log

# ComfyUI / pipeline scratch
*.local.mjs
